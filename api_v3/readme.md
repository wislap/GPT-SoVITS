# 启动流程

1. > 使用conda等等创建python环境  
   > 使用gpt-sovits相同也可以
2. python -m api_v3.server -p 9881       # 启动api_v3服务
3. 打开frontend文件夹 执行npm install,然后npm run dev
4. 浏览器打开网页,进行配置
5. 打开neko界面,点击api密钥->高级api配置->最底部GPT-sovits配置