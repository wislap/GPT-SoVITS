# 启动流程

1. > 使用conda等等创建python环境  
   > 使用gpt-sovits相同也可以
   > 进入到gpt-sovits的目录下,解压api_v3.zip
2. python -m api_v3.server -p 9881       # 启动api_v3服务
3. 浏览器打开网页: 127.0.0.1:9881 进行配置 
4. 打开neko界面,点击api密钥->高级api配置->最底部GPT-sovits配置
